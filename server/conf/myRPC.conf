# myRPC.conf -- configuration of myRPC-server.
# The file is read at startup and on SIGHUP.

# TCP or UDP port the server listens on.
port = 1234

# Transport: stream (TCP) or dgram (UDP).
#socket_type = dgram
socket_type = stream

# Run as a daemon (yes) or in the foreground (no).
daemon = yes
